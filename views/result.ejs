<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Quiz Result</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/png" href="/logo.png" />
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <style>
    .progress-bar {
      animation: fillProgress 1s ease-out forwards;
    }
    @keyframes fillProgress {
      from { width: 0; }
    }
  </style>
</head>
<body class="bg-gradient-to-br from-purple-50 to-gray-100 flex items-center justify-center min-h-screen">

  <div class="bg-white shadow-2xl rounded-2xl p-10 max-w-md w-full text-center">
    <h1 class="text-4xl font-extrabold text-purple-700 mb-3">Quiz Completed!</h1>
    
    <p class="text-gray-700 text-lg mb-6">
      You scored <span class="text-blue-600 font-bold text-xl"><%= score %></span> out of 
      <span class="text-green-600 font-bold text-xl"><%= total %></span>
    </p>

    <% const percentage = Math.round((score / total) * 100); %>

    <div class="w-full bg-gray-200 rounded-full h-4 mb-4 overflow-hidden">
      <div class="bg-gradient-to-r from-green-400 to-green-600 h-4 progress-bar rounded-full transition-all duration-700 ease-out" 
           style="width: <%= percentage %>%"></div>
    </div>

    <p class="text-sm text-gray-500 mb-6"><%= percentage %>% Correct</p>

    <a href="/" class="inline-block mt-2 bg-purple-600 hover:bg-purple-700 text-white px-6 py-2 rounded-full font-medium shadow">
      Back to Home
    </a>
  </div>
<% if (typeof message !== 'undefined') { %>
  <div id="popup-msg" class="fixed bottom-5 right-5 bg-purple-600 text-white px-4 py-2 rounded shadow z-50 animate-bounce">
    <%= message %>
  </div>
  <script>
    setTimeout(() => {
      document.getElementById('popup-msg')?.remove();
    }, 3000);
  </script>
<% } %>

</body>
</html>
